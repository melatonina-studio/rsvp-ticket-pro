// Event Status
export type EventStatus = "draft" | "published";

// Payment Status
export type PaymentStatus = "pending" | "paid" | "refunded" | "free";

// Ticket Status
export type TicketStatus = "active" | "used" | "cancelled";

// Ticket Access Mode
export type TicketAccessMode = "free" | "door" | "paid";
